const BaseballQuiz = require('./game.js');


test('Returns a random position', () =>{
    let baseballQuiz = new BaseballQuiz();
    let randomAnswer = baseballQuiz.randomAnswer;
    expect(randomAnswer).toBe(randomAnswer);
});


test('Returns a random position', () =>{
    let baseballQuiz = new BaseballQuiz();
    let randomAnswer = baseballQuiz.randomAnswer;
    expect(randomAnswer).toBe(randomAnswer);
});
